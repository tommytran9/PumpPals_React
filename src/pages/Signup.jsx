

// Same thing as LoginForm; the only difference here is that we'll have an extra input that is used for
// validating the confirmed password just to make sure that they match.
function Signup() {
    return <h1>this is the signup page</h1>
}

export default Signup;
